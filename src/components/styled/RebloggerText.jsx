import styled from 'styled-components/native';
import AppText from './AppText';

const RebloggerText = styled(AppText)`
  /* TODO: font size from device config! */
  font-size: 15px;
`;

export default RebloggerText;
