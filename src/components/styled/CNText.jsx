import styled from 'styled-components/native';
import AppText from './AppText';

const CNText = styled(AppText)`
    /* TODO: font size from device config */
    font-size: 22px;    
`;

export default CNText;
