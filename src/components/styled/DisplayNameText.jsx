import styled from 'styled-components/native';
import AppText from './AppText';

const DisplayNameText = styled(AppText)`
  /* TODO: font size from device config! */
  font-size: 18px;
`;

export default DisplayNameText;
