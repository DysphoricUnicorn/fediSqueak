import styled from 'styled-components/native';
import HTMLStyleImage from '../HTMLStyleImage';

const TinyImage = styled(HTMLStyleImage)`
  height: 15px;
  width: 15px;
  margin-right: 2px;
`;

export default TinyImage;
