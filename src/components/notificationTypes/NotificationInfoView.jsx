import styled from 'styled-components/native';
import MetadataContainer from '../styled/MetadataContainer';

const NotificationInfoView = styled(MetadataContainer)`
  padding: 0 5px 0 5px;
`

export default NotificationInfoView;
